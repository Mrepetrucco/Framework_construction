# 20260803_2353_mod_all_standing-rules_v0.2
## R-UNREC (owner ruling, 3 Aug): UNRECONCILED BY DEFAULT
Every new claim enters `X1_catalogue/unreconciled/` by default. It graduates to `reconciled/` ONLY on:
(a) a deterministic/measured result with its evidence file named, OR
(b) cross-FAMILY convergence (same-family agreement is SCREENING, never grounding), OR
(c) an explicit owner ruling.
Rationale (measured pattern across this build): the recurring failure is a single measurement generalised into a
rule. Default-unreconciled makes that failure structurally visible instead of silently shipped.

## R-TIER: highest-tier-only for grounding/adjudication/battle; test-SUBJECT role uses deployment tier or sweeps.
## R-BILL: never bill the runner's work to the prompt.
## R-SELF: a producing component never grades its own product; declare interested-party status when unavoidable.
## R-BOUND: Fable max_tokens >= ~14k hidden reasoning + 2x visible [MEASURED x3]. terra empty at 4k cap.
## R-CRED: Github_key3.env is the write credential (bare token, one line). The token inside ENVs.zip is read-only.
