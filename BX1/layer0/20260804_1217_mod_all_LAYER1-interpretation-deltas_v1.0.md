# 20260804_1217_mod_all_LAYER1-interpretation-deltas_v1.0
**THIS IS NOT GROUND TRUTH.** Each entry is CLAUDE'S READING of one L0 id. Never merge into layer 0. Each carries
the L0 id it interprets, the delta (what was added beyond the owner's words), and its enforcing mechanism.
Rule: a delta validates against its L0 id ONLY — never against another delta (star-grounding, not chain).

| L0 id | interpretation (mine) | DELTA — what I added | enforced by |
|---|---|---|---|
| D3 | multi-match = the CORRELATION anomaly class; present the co-firing group unranked | mapped owner's ruling onto the firewall-anomaly taxonomy; owner did not name that taxonomy | X2R conflict discovery |
| D5 | T-DIRECT / T-ADJACENT / T-LATENT tiers computed against knowledge-graph origin | invented the three tier NAMES and the reversibility rule; owner said "tiered-application" + "direct hit" only | X1 tiering |
| D6 | four-axis class set (intrinsic/extrinsic · factuality/faithfulness · manifestations · causal origin) | selected WHICH reputable sources; owner specified only "exhaustive, reputable, no discard" | fabrication class set |
| D7 | do not probe an uncertain referent, because probing manufactures it | owner said raise instead of "having concern to test context"; the causal mechanism claim is MINE | uncertainty raise rule |
| D9 | scope-subset / new-noun / owner-verifiability checks | the three checks are Fable's formalisation, not the owner's words | question-scope discipline |
| D2 | "controlled not true" as an error-handling requirement on every class | applied it as a design law across all X-artifacts; owner stated it as a property of governance | all machinery |
| D15 | star-grounding + type-not-depth termination | entirely derived; owner asked HOW FAR, did not propose a topology | this restructure |
| A5 | numbered plain-text questions, never a picker | "never a picker" is a stored preference, consistent with A5 but not stated in A5 | session-open |

## KNOWN PARAPHRASE-CONTAMINATION (the drift risk, declared)
Most of what is currently in memory and in BX1/X1_catalogue is MY WORDING of owner rulings, stored without the
utterance beside it. Under star-grounding those entries are chain-grounded and their fidelity is unverifiable.
REMEDIATION: entries are re-pointed to an L0 id as they are next touched; any entry that cannot be re-pointed to
an L0 id is marked ORPHANED and treated as my claim, not the owner's.
