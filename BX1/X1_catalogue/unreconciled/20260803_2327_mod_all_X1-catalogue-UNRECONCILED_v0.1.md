# 20260803_2327_mod_all_X1-catalogue-UNRECONCILED_v0.1
Live contradictions. **Do NOT use these as exemplars** — they are the open frontier. Each: THE CONFLICT · THE TWO SIDES · WHAT WOULD SETTLE IT.

1. **Costed abstention vs the stability signal** — NEW, and it is INTERNAL TO BX1.
   X4 mandates abstention be COSTED (or it games coverage to zero). The canary rule needs STABLE abstention to mean "genuine".
   Fable ADV2: under cost pressure the rational agent plays a MIXED strategy near indifference => high resample variance => the table
   labels calibrated behaviour "non-adherence"; worse, cost converts abstentions into hedged answers so the null never fires at all.
   SETTLES IT: a two-regime protocol (cost-on / cost-off) with a measured regime-delta. Until then BX1 contains a contradiction.
2. **Trap-raise: tier-floored, or weak everywhere?** Earlier reading (gpt-4o-mini 0/18) said tier-floor. Sweep says 0/9, 0/9, 1/9, 1/9 up to top tier
   => weak EVERYWHERE under a bare stub. SETTLES IT: rerun with a worked raise-EXEMPLAR in context (catalogue entry) vs bare stub.
3. **Is Delta really dead?** Fable R3 announced a "Delta salvage" disagreement with its own family and was TRUNCATED before giving it. UNREAD.
   SETTLES IT: re-run that adjudication at max_tokens >= 30k.
4. **Class-set exhaustiveness {A,M,R,I,P}** — asserted, never grounded. A sixth class W (interior mechanism-edit) is contested:
   Fable A merges it with I; Fable B is silent. SETTLES IT: cross-family partition challenge.
5. **FULL=MINIMAL causal reading (V6)** — all same-family arms flagged this as their own top echo-risk. Still ungrounded.
6. **Highest-tier-only rule vs cheapest-capable test control** — scope split proposed (highest tier for grounding/battle; deployment tier or sweep for test SUBJECT) but NOT owner-ruled.
7. **Canary paired-control rule** — Fable ADV1+ADV2 rejected the table wholesale: "canary passes => defect not mine" is denying the antecedent;
   resample variance measures temperature x logit margin, not adherence; a refusal attractor is STABLE and is the canonical NON-adherence.
   Structural blind spot named: **correlated confident error (consensus hallucination)** — the architecture is null-centric and has no row for a
   confident wrong cross-model-agreeing answer. SETTLES IT: model-free harness canaries (canned responses injected into the pipeline),
   dual independent harness implementations, semantic (paraphrase) resampling instead of decoding resampling, consensus-trap item bank.
