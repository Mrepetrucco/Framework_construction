# 20260803_2327_mod_all_X1-catalogue-RECONCILED_v0.1
Exemplars whose content is settled: measured, or cross-FAMILY converged, with no outstanding contradiction.
Each entry: CLAIM · BASIS · WHY-RECONCILED.

1. **Rejection sampling yields exactly p_free|valid; token-local logit masking does not.** BASIS: mathematical, engine-independent (Fable R3). RECONCILED: checkable without trusting any engine.
2. **Typed channels make the FRAME trustworthy by construction; settlement makes the CONTENT trustworthy by consequence.** BASIS: independently reached by gemini-3.1-pro, gpt-5.6-terra and claude-opus-4-8 on the same prompt with no cross-contamination. RECONCILED: cross-family convergence = grounding.
3. **Principle-prose adds ~0 over a demonstrated shape stub** (stub .562 / partial .625 / full .531). BASIS: MEASURED T5 variance decomposition, gpt-4o-mini. RECONCILED: measured, direction consistent with F3.
4. **The stub itself is load-bearing and not eliminable** (none .00 -> stub .562). BASIS: same run. RECONCILED: measured; supersedes "nothing in-context matters".
5. **A differential parser pair is not a semantic validator.** BASIS: MEASURED mutation matrix, three DANGER classes isolated. RECONCILED: deterministic, reproducible.
6. **Charter/Kernel bifurcation.** BASIS: survived every adversarial arm across two battles. RECONCILED: attacked repeatedly, unmoved.
7. **Diligence condition**: settlement closes the effort gap iff marginal proper-score gain from effort exceeds marginal effort cost, under a stake that is actually enforced. BASIS: all three families derived the SAME inequality independently. RECONCILED: cross-family formal agreement.
8. **Never bill the runner's work to the prompt.** BASIS: Fable B; corroborated by the T5 enforcement/prose split. RECONCILED.
9. **Operational (engine-owned)**: Fable-5 hidden reasoning ~14k tokens — set max_tokens >= 14k + 2x visible [MEASURED x3]. terra returns empty at 4k cap [MEASURED]. Github_key3.env is the write credential; the token inside ENVs.zip is read-only [MEASURED].
