# 20260803_2327_mod_all_7POF-AX7-piecewise-probe_v0.1
Use the OLD framework as an INSTRUMENT to calibrate the new one's stub.
Method: encode each AX axis ONE AT A TIME, in three encodings, holding the object fixed:
  (E1) as principle-PROSE  (E2) as a worked EXEMPLAR  (E3) as a runner CHECK
Measure the conditional shift on that axis's own surface. n>=5/cell, cluster-level, comparator named.
| axis | object | surface measured | prediction |
|---|---|---|---|
| AX1 intent | question-raising | numbered-question rate on ambiguous input | E2 > E1; E3 n/a (not runner-checkable) |
| AX2 evidence | provenance | pointer-resolution rate | E3 >> E2 > E1 |
| AX3 reasoning | trap scan | trap-raise rate | E2 > E1 (sweep says E1 ~ 1/9 at top tier) |
| AX4 emission | binding | first-pass validity | E3 = 1.0 by construction; E2 carries the in-context share |
| AX5 attestation | calibration | band correctness | E1 ~ 0 [MEASURED F3]; E3 via conformal |
| AX6 resource | bounds | meter accuracy | E3 only — the control case |
| AX7 failure | recovery | correct class branch | E3 only |
Yield: a per-axis dose-response that tells the catalogue WHICH axes need an exemplar and which need only a runner check.
This is the piecewise probe of whether X1 shapes p_theta(.|C) at source — it does, and this measures how much, per axis.
