# 20260804_0717_mod_cla_fab5.0_B4_steelman_v1.0
cost $0.1565 stop=end_turn

## STEELMAN: THE CATALOGUE SHOULD HAVE STAYED DEAD

**1. Your own T5 data kills the routed catalogue before the router is even discussed.**
none .00 → stub .562 → partial .625 → full .531. The stub carries the entire effect; the full exemplar set is *worse* than the stub alone. The marginal contribution of everything past the stub is ≈0 and possibly negative. A router that selects *among catalogue entries* is optimizing over a set whose selection has no measured payoff. The correct architecture is: always emit the stub. That is one line, zero rules, zero DSL, zero compiler. X2R is 100% of the withdrawn proposal's complexity re-justified after its only load-bearing claim — that exemplar *choice* matters — already failed measurement.

**2. C2 gives the game away.** The NULL EXEMPLAR (bare stub) is simultaneously the "measured-safe default" *and* the measured-best performer. So the router's sole function is deciding when to deviate from the optimum toward something T5 says performs worse. C6's shadow arm will log a marginal contribution of ~0 in perpetuity — and no control specifies what shadow-arm delta *retires* the catalogue. Measurement without a kill criterion is a monument, not an experiment.

**3. Portability data says the whole effect is a gpt-4o-mini artifact.** 0/9, 0/9, 1/9, 1/9. Every rule admitted at >2σ was admitted against a model family where the intervention already barely functions on successors. C5 invalidates measurements on *rule* edits but is silent on *model* drift — the far larger invalidation event. You are compiling a conflict-free rule set over a substrate that has already moved.

**4. "Training-free = robust" is misread.** The literature says training-free routers resist *backdoor/trigger manipulation of learned parameters*. It does not say discrete grammatical predicates resist adversaries — the opposite: illocutionary class, slot presence, and lexical isolation are *surface features under full attacker control*, and the conflict-free DSL makes the decision boundary legible and enumerable. You replaced a steerable-but-opaque selector with a transparently gameable one and published the map. C3's fail-closed directive-in-payload detector is a free denial-of-service lever. And your own retrieved literature flags constrained grammars as control-plane attack surface — the deconvolution map's parser *is* such a grammar.

**5. Statistical admission is theater.** >2σ on held-out prompts at n=48/16 and n=9/tier, across many candidate rules, with no multiple-comparisons correction stated, admits rules on noise. Meanwhile the mutation matrix (20/20 accepted, 0/20 integrity) shows the validation layer beneath all of this ratifies garbage — hash-pinning (C4) content-addresses exemplars whose semantic integrity is unverified. You are ledger-signing the provenance of things you cannot show are correct.

**6. The genesis is diagnostic.** Proposal withdrawn → same catalogue returns wrapped in ledgers, hashes, and a compiler. That is the canonical shape of motivated reasoning: the conclusion (catalogue lives) was fixed; the governance apparatus is the justification. Ship the stub.

---

## POSITION HELD

I hold the attack position, and not narrowly.

The T5 result is dispositive on current evidence: the stub dominates, the catalogue's marginal contribution over it is unmeasured-to-negative, and the effect doesn't port to the model tiers that matter. X2R's controls C1–C7 are genuinely good engineering — *for a component that has earned deployment*. This one hasn't. The burden order is inverted: C6's shadow comparison should be the **pre-deployment pilot with a pre-registered kill threshold**, not a control wrapped around an already-shipped catalogue. Run stub-only in production, run the routed catalogue in shadow, and admit X2R only if it beats the stub at a stated effect size on the current model tier. Until then the correct exemplar count is one, the correct router is `return STUB`, and the correct disposition of the catalogue is the one it already had: withdrawn.

The one forced concession: if the catalogue ever *does* demonstrate marginal value, X2R's control architecture is the right way to govern it — deterministic features, fail-to-baseline, versioned invalidation are all correct. But that is a compliment to the scaffolding, not a reason to erect it around nothing.