# 20260804_0717_mod_cla_fab5.0_B2_deconv_v1.0
cost $0.1417 stop=refusal

## X2R-1: the front-end is a parser or it is wrong. Pick one.

**Meta-attack first.** Every feature below requires syntactic/semantic analysis at a depth where rule-based extraction on open text collapses. Any extractor accurate enough to be "useful" is a tagger/parser — a learned or heavily-curated component. You have reintroduced the withdrawn flaw one layer down: the DSL is conflict-free over the feature *vector*, but the vector is produced by a noisy front-end, so conflict-freedom in feature space guarantees nothing in prompt space. Two paraphrases yielding different vectors give the attacker a **differential oracle** against the router — cheaper to probe than the embedding selector was.

## Per-feature breaks

**Illocutionary class.** You are classifying *mood*, not *force*. Force is pragmatic and not surface-decid