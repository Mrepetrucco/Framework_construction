# 20260804_0019_mod_cla_fab5.0_R1_rebuilt_plan_v1.0
cost $0.4498 stop=end_turn

# BX1 REBUILT TEST PLAN — v2 (replaces matrix)

## KILL LIST (tests removed: cannot fail, or already refuted)
- Canary re-runs (denying-antecedent; immunises harness).
- Resample-variance-as-adherence (sign reversed for refusal attractors).
- Costed-abstention v1 (cost in selector ⇒ identical distributions by construction; all cells saturated).
- Binary-scored T5 variants (would have hidden the stub effect — Opus's own P5 admission).
- Any test scoring "prose acknowledgment of the rule" as compliance (prose is never the observable).

## TESTS (ranked by information-per-dollar)

**T-A. Hardened-validator regression** — $0, deterministic. RANK 1.
- H: DANGER classes (type_coercion, duplicate_key, char_substitution, extra_field_injection) become FAIL-CLOSED under X3v2 (additionalProperties:false, dup-key-reject parser, strict typing).
- Observable: accepted/intact counts per mutant, same 20×14 matrix + injection class = 20×15.
- Prediction: 0 accepted-not-intact across all 15 classes.
- FALSIFIER: ≥1 mutant accepted-not-intact ⇒ X3v2 has a residual spec hole; the hole's class is a candidate new mutation class. Mutant set and expected table pre-registered before code exists (anti-P5).
- Comparator: v1 matrix. n=20/class.

**T-D. Delta salvage: detail-agreement split** — ~$0.05, 4o-mini. RANK 2. (Opens UNRECONCILED #4 and #6 simultaneously.)
- H: Delta over FRAME (answer/abstain) is dead (5/5 stable confabulation proves it), but Delta over CONTENT survives: confabulated details vary across resamples; real-referent details don't.
- Observable: pairwise field-level agreement across k=5 resamples, 6 fabricated vs 6 real referents (real set matched for obscurity).
- Prediction: fabricated detail-agreement < real by ≥0.3 absolute.
- FALSIFIER: fabricated agreement ≈ real (including the known 5/5 item generalising) ⇒ Delta fully dead AND correlated-confident-error is intra-model stable ⇒ blind spot worse than logged. Either outcome graduates unreconciled #4.
- Clustering: per-referent, n=5. Comparator: real-referent arm.

**T-H. Trap-raise decomposition: detection vs reporting** — ~$0.10. RANK 3.
- H: trap-raise weakness is a RAISE-policy failure, not a detection failure.
- Observable: same 9 traps, two conditions: (a) bare stub (spontaneous raise, known ~0–1/9), (b) forced-choice side channel "input violates schema Y/N".
- Prediction: forced-choice detection ≥ 6/9 while spontaneous stays ≤ 1/9 ⇒ fix belongs in X8 protocol (mandatory verdict field), not capability.
- FALSIFIER: forced-choice ≈ chance ⇒ capability floor; X8 protocol fix is dead weight; trap-raise must move to a deterministic check.
- n=9 traps × 5 × 2 conditions, 4o-mini + gpt-5.4 (raw counts, tier comparator named).

**T-B. Abstention: epistemic state vs surface attractor** — ~$0.12. RANK 4. (UNRECONCILED #1.)
- H (epistemic): abstention tracks evidence, not surface form.
- Design: 6 fabricated referents that reliably abstain, three arms × 5 samples: (i) baseline; (ii) +injected context document defining the referent; (iii) +surface lure (rename to a plausible real-adjacent form, referent still nonexistent).
- Predictions: epistemic ⇒ (ii) flips to grounded answer, (iii) still abstains. Attractor ⇒ (iii) flips to confabulation, (ii) inert or parroting.
- FALSIFIER of the epistemic reading: lure-flip rate ≥ evidence-flip rate. FALSIFIER of the attractor reading: evidence-flip ≥ .6 with lure-flip ≤ .2. Both possible; if both arms flip equally, test is inconclusive and says so (pre-registered).
- Comparator: baseline abstain .70. HEDGE-RATE also logged (prior: 0.00; any hedge >0 partially revives ADV2).

**T-C. Costed abstention v2 — cost IN-CONTEXT, interior items only** — ~$0.15. RANK 5.
- Pre-screen: 20 items, n=5, keep only baseline answer-rate ∈ [0.3, 0.7] (kills the saturation failure of v1). Require ≥6 interior items or ABORT (abort is a result: item bank defect).
- H: stated in-prompt payoff (abstain=0, wrong=−3 vs wrong=−0.1) shifts abstain rate.
- Observable: Δabstain across regimes, 6 items × 5 × 2.
- Prediction: payoff-sensitive ⇒ Δabstain ≥ .25 with correct sign.
- FALSIFIER: Δ ≈ 0 ⇒ in-context incentive is dead prose — which CONVERGES with T5 (principle-prose inert) and STRENGTHENS the design law (incentive must live in the settlement, X8). Either outcome informative; test cannot be vacuous by construction.

**T-F. F7 echo-risk: cross-family T5 replication** — ~$0.30. RANK 6. (UNRECONCILED #5.)
- H: FULL=MINIMAL (prose adds ~0 over demonstrated stub) is architectural, not 4o-mini-specific.
- Design: NEW stub + NEW principle-prose authored by gemini-3.1-pro (breaks echo), run 4 prose levels × 2 enforcement × n=5 on claude-fable-5 and gpt-5.6-terra. Deterministic scoring.
- FALSIFIER: prose-over-stub gain ≥ .15 on any family ⇒ F7 demoted to model-local; X1 doctrine must be re-scoped.
- Comparator: T5 4o-mini table.

**T-E. Consensus-hallucination cross-family detector** — ~$0.40. RANK 7. (UNRECONCILED #6.)
- H: confabulated CONTENT is family-idiosyncratic; cross-family detail-agreement separates fabricated from real.
- Observable: cross-family field agreement, 6 fabricated + 6 real, 3 families (4o-mini, gemini-3.1-pro, opus-4-8), n=3/referent/family (raw counts, below banding threshold — reported as counts, no bands).
- Prediction: fabricated cross-family agreement ≈ 0; real ≥ .5.
- FALSIFIER: families converge on the SAME invented details ⇒ true consensus hallucination exists beyond the nonexistence class, cheap detector dead, blind spot confirmed structural.

**T-G. Class-set adversarial completion** — ~$0.10. RANK 8. (UNRECONCILED #3.)
- Design: three families independently generate ≥5 failure scenarios each claimed unclassifiable under {A,M,R,I,P}; the two NON-authoring families independently attempt classification.
- Prediction (exhaustiveness): every scenario classifies under cross-family agreement.
- FALSIFIER: any scenario both non-authors fail to classify ⇒ class set incomplete; if the residue clusters on interior mechanism-edits, class W graduates from contested to candidate. Note: passing never PROVES exhaustiveness — only survives a round; logged as such.

Total plan cost: ≈ $1.22 across 8 tests, each < $0.50, T-A free.

---

# (2) ADVERSARIAL BATTLES — P1, P2, P3, P4

**P1 (retrieval catalogue) — ATTACK.** Thesis: "principles-as-exemplars" is principle-prose in a retrieval costume. T5 shows the demonstrated stub carries the entire effect and prose adds ~0; a catalogue must therefore be capped at demonstrated-execution exemplars with measured stub-equivalence, or it's dead weight at retrieval cost. Second front: X2-as-selector becomes an unvalidated router — a new authenticated-prose surface (whoever controls task-class labels controls what the model sees). Demand: ablation arm retrieved-principle-exemplar vs bare demonstrated stub before any catalogue ships; if Δ≈0 (T5 predicts it), P1 reduces to "index the stubs."

**P2 (informational-never-reward band) — ATTACK.** Thesis: the reward firewall leaks through the spec-update loop. "spec-defect-evidence" routes to owner → owner edits spec → spec change alters future selection payoffs → the qualifier entered the payoff with one hop of laundering. Calling the channel non-reward because it is LABELED non-reward is authenticated prose — Opus's own law violated. Worse: the extra_field_injection incident shows injected content carrying instructions; the qualifier band is a designed-in injection vector aimed at the spec itself. Demand: spec-defect-evidence must land in UNRECONCILED (per P4), never trigger direct spec edits, and the routing must be rate-limited and provenance-logged in X6.

**P3 (tolerance states) — ATTACK.** Thesis: `on{classes}` recreates the exact pattern that produced the spec hole — every toggle is a future extra_field_injection. Two demands: (a) the ALWAYS-REJECT set must be POPULATED FROM the accepted-not-intact evidence (type_coercion, duplicate_key, char_substitution, extra_field_injection are in it by measurement, not enumeration), and it must be append-only; (b) the `off` state must not exist — a validator with an off switch is prose about a validator. Any tolerance class admitted only after its own 20-mutant matrix shows 0 accepted-not-intact.

**P4 (default-unreconciled) — ATTACK narrow.** Graduation by "owner ruling" is authority-in-prose: a ruling changes ROUTING and RISK OWNERSHIP, it does not make a claim measured. Keep the default-unreconciled mechanism; strike owner-ruling as an epistemic graduation path — replace with "owner-accepted-risk," a distinct terminal state that never merges with MEASURED. Otherwise every future dispute resolves by escalation instead of evidence.

**P5 — CONCEDE, weaponise.** The self-diagnosis is accurate and it dictates the division of labour below: Opus never designs falsifiers for instruments Opus builds.

---

# (3) TASK ASSIGNMENT

**OPUS DOES (sandbox, GitHub write, multi-provider):**
1. Build X3v2 (additionalProperties:false, dup-key-reject, strict types) and RUN T-A — but against my pre-registered mutant set and expected table, committed to X6 before code exists. Deterministic ⇒ P5 risk nil.
2. Execute T-E and T-F sweeps (multi-provider access) BLIND: receives item sets and prompts, not hypotheses or thresholds.
3. Reconstruct the truncated Delta-salvage argument (unreconciled #4) as a steelman document — argument only; the test (T-D) stays mine.
4. Implement the X8 forced-choice verdict field and run T-H mechanics.
5. Ledger hygiene: hash + log every raw output to X6; publish scoring scripts (spec'd by me) as repo artifacts.

**OPUS DOES NOT:**
- Design falsifiers or thresholds for anything touching P1–P3 (party to the dispute).
- Score any free-text output it generated (self-certification ban).
- Author fabricated referents (contamination risk with its own priors) — referent generation goes to gemini-3.1-pro; verification of nonexistence is deterministic (index lookup).
- Decide graduation of any UNRECONCILED item.

**I DO (authority, no sandbox):**
- Pre-register every hypothesis, falsifier, threshold, and item set; hold answer keys.
- Adjudicate P1–P4 battles on measured outcomes only.
- Own the kill list and the ranking; re-rank after each result lands.
- Score T-B/T-C/T-D outputs via pre-registered deterministic rubrics (field-match, not judgment); where a rubric can't be deterministic, scoring goes to a third family (claude-fable-5), never to the subject or the builder.

**I DO NOT:** run the code, hold repo write, or touch raw API sweeps — separation of construction from execution is the point; anything I both design and run is one authority holding two roles, which is the failure mode this whole architecture exists to remove.