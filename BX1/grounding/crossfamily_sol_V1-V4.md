# GPT-5.6-SOL CROSS-FAMILY VERDICTS ($0.011 measured)

- **V1 — QUALIFY.** The T0/T1/T2 decomposition is sound, but “only T0 can be grammatical” is too strong. Trusted parsers, schemas, provenance labels, capability types, proof-carrying inputs, or decidable policy logics can enforce typed precedence over richer domains than lexical patterns. Conversely, credential-shaped strings establish morphology, not credential status. Open-world semantic predicates do remain behavioural obligations unless reduced to an enforceable formal interface; broader *unenforced* typing is indeed merely annotative prose.

- **V2 — QUALIFY.** Abstention is self-reported behavior, not independent evidence of correctness; externally calibrated bands can supply evidence under their coverage assumptions. But assigning non-abstention “zero evidential weight” does not by itself solve selective behavior: abstention and answering jointly determine the evaluated distribution. Under rejection or routing, coverage can collapse unless the target estimand includes abstentions—e.g., abstention receives a loss/cost—or coverage is explicitly calibrated for the selected population. Scoring abstentions is necessary in many such environments, but not sufficient under adaptive selection or distribution shift.

- **V3 — QUALIFY.** Runner-controlled assembly can guarantee **syntactic provenance** if payloads are encoded as data using an unambiguous parser/serializer boundary. Textual canonicalization or escaping alone is weaker: Unicode normalization, later decoding, tokenizer effects, and semantically equivalent “fake wrappers” can still influence the model. Thus authentic-by-construction wrappers require structural channels and verified end-to-end parsing, not merely escaped prompt text. Even then, authenticity does not imply model compliance, so behavioral measurement remains necessary. Claim authority should indeed come from the runner’s ledger rather than in-band assertions.

- **V4 — Not unconditionally; post-selection calibration is sufficient only under renewed exchangeability.** If a fixed selection rule is applied independently and identically to calibration and production examples, and both are exchangeable draws from the same conditional distribution \(P(X,Y\mid S=1)\), ordinary conformal guarantees can hold **marginally over accepted traffic**. Merely saying both datasets are “validated” is insufficient when selection is adaptive, calibration-dependent, label/score-dependent in a mismatched way, or production drifts. It also does not recover guarantees for rejected traffic, the pre-selection population, or arbitrary subgroups. Selection uncertainty and smaller effective calibration size must likewise be accounted for.